export const host = "http://localhost:8088";

export const authenticationRoute = `${host}/authentication`;
export const welcomeRoute = `${host}/`;

export const profileRoute = `${host}/profile`;
export const contactRoute = `${host}/contacts`;

export const conversationRoute = `${host}/conversations`;
export const messageRoute = `${host}/messages`;

export const interestRoute = `${host}/interests`;
export const userInterestRoute = `${host}/user-interests`;
